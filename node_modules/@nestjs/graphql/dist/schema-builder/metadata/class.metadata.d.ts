import { DirectiveMetadata } from '@nestjs/graphql/lib/schema-builder/metadata/directive.metadata';
import { PropertyMetadata } from '@nestjs/graphql/lib/schema-builder/metadata/property.metadata';
export interface ClassMetadata {
    target: Function;
    name: string;
    description?: string;
    isAbstract?: boolean;
    directives?: DirectiveMetadata[];
    extensions?: Record<string, unknown>;
    properties?: PropertyMetadata[];
    inheritDescription?: boolean;
}
//# sourceMappingURL=class.metadata.d.ts.map